FIXED VERSION

Upload ALL 6 files in this folder directly to the root of your GitHub Pages repository. Do not upload the outer folder itself. The badge front/back are embedded inside index.html, so they cannot break from missing asset paths.

After deployment, refresh Safari. If an older installed Home Screen app is cached, delete it from the Home Screen, open the site in Safari once, then Add to Home Screen again.
